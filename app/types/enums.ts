export enum SubscriptionStatus {
	INACTIVE = "inactive",
	ACTIVE = "active",
	CANCELLED = "cancelled",
	PENDING = "pending",
	EXPIRED = "expired",
}

export enum SubscriptionType {
	FREE = "free",
	PREMIUM = "premium",
}

export enum UserRole {
	MUSICIAN = "musician",
	VOCALIST = "vocalist",
	SOUND_ENGINEER = "sound_engineer",
	EQUIPMENT_MERCHANT = "equipment_merchant",
}
